<?php

/* require_once 'includes/app.php';

require_once '../bienesmvc/includes/config/database.php';
$db = conectarDB();

$email = 'correo@correo.com';
$password = '123456';
$passwordHash = password_hash($password, PASSWORD_BCRYPT);

$query = " INSERT INTO usuarios (User_Nombre, User_Password) VALUES ('${email}', '${passwordHash}'); "; */     /* echo $query; */

/* mysqli_query($db, $query);

mysqli_close($db); */
?>